apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: progetdata-claim
  namespace: opstools
spec:
  storageClassName: nfs-proget
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Gi