elasticsearch:
  replicas: 3
  minimumMasterNodes: 2
  clusterName: apim-es
  persistence:
    enabled: true
  volumeClaimTemplate:
    accessModes: ["ReadWriteOnce"]
    resources:
      requests:
        storage: 30Gi
    storageClassName: "nfs-client"
  envFrom:
  — secretRef:
      name: elastic-secret
  protocol: http