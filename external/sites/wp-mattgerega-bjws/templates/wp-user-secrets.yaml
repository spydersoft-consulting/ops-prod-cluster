apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wp-external-secret-user-bjws

spec:
  refreshInterval: 1h0m0s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: wp-mg-secrets-bjws
    template:
      data:
        WORDPRESS_PASSWORD: "{{ .password }}"
  data:
  - secretKey: password
    remoteRef:
      key: secrets-k8/wordpress/admin
      property: password
