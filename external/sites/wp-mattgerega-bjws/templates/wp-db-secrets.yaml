apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wp-external-secret-db-bjws

spec:
  refreshInterval: 1h0m0s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: wp-mg-dbsecrets-bjws
    template:
      data:
        MARIADB_PASSWORD: "{{ .password }}"
        MARIADB_REPLICATION_PASSWORD: "{{ .rootpassword }}"
        MARIADB_ROOT_PASSWORD: "{{ .rootpassword }}"
  data:
  - secretKey: password
    remoteRef:
      key: secrets-k8/wordpress/database
      property: password
  - secretKey: rootpassword
    remoteRef:
      key: secrets-k8/wordpress/database
      property: rootpassword
