apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: wp-external-secret-db

spec:
  refreshInterval: "10s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: wp-mg-dbsecrets
    template:
      engineVersion: v2
      metadata:
        labels:
            mylabel: test
      data:
          mariadb-password: '{{ .password }}'
          mariadb-replication-password: '{{ .rootpassword }}'
          mariadb-root-password: '{{ .rootpassword }}'
  data:
  - secretKey: password
    remoteRef:
      key: secrets-k8/wordpress/database
      property: password
  - secretKey: rootpassword
    remoteRef:
      key: secrets-k8/wordpress/database
      property: rootpassword