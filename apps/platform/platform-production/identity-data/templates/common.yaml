---
# Source: identity-data/templates/common.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-data
  labels:
    app.kubernetes.io/instance: identity-data
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-data
    app.kubernetes.io/version: 0.0.0
    helm.sh/chart: identity-data-0.1.0
  namespace: platform-production
data:
  init-data.sh: |
    #!/bin/bash
    set -e;
    if [ -n "${POSTGRES_NON_ROOT_USER:-}" ] && [ -n "${POSTGRES_NON_ROOT_PASSWORD:-}" ]; then
      psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
        CREATE USER "${POSTGRES_NON_ROOT_USER}" WITH PASSWORD '${POSTGRES_NON_ROOT_PASSWORD}';
        GRANT ALL PRIVILEGES ON DATABASE ${POSTGRES_DB} TO "${POSTGRES_NON_ROOT_USER}";
        ALTER DATABASE ${POSTGRES_DB} OWNER TO "${POSTGRES_NON_ROOT_USER}";
    EOSQL
    else
      echo "SETUP INFO: No environment variables given!"
    fi
---
# Source: identity-data/templates/common.yaml
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: identity-data
  labels:
    app.kubernetes.io/instance: identity-data
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-data
    app.kubernetes.io/version: 0.0.0
    helm.sh/chart: identity-data-0.1.0
  namespace: platform-production
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "300Gi"
  storageClassName: "nfs-client"
---
# Source: identity-data/templates/common.yaml
apiVersion: v1
kind: Service
metadata:
  name: identity-data-postgres
  labels:
    app.kubernetes.io/instance: identity-data
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-data
    app.kubernetes.io/service: identity-data-postgres
    app.kubernetes.io/version: 0.0.0
    helm.sh/chart: identity-data-0.1.0
  namespace: platform-production
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/controller: postgres
    app.kubernetes.io/instance: identity-data
    app.kubernetes.io/name: identity-data
---
# Source: identity-data/templates/common.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-data-postgres
  labels:
    app.kubernetes.io/controller: postgres
    app.kubernetes.io/instance: identity-data
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-data
    app.kubernetes.io/version: 0.0.0
    helm.sh/chart: identity-data-0.1.0
  namespace: platform-production
spec:
  revisionHistoryLimit: 1
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/controller: postgres
      app.kubernetes.io/name: identity-data
      app.kubernetes.io/instance: identity-data
  template:
    metadata:
      annotations: 
        checksum/configMaps: ed74e4086bff9b5df5fd05049cade90ad26b5c6600f694e6cb9bffdd7ebd3b52
        linkerd.io/inject: disabled
      labels: 
        app.kubernetes.io/controller: postgres
        app.kubernetes.io/instance: identity-data
        app.kubernetes.io/name: identity-data
    spec: 
      enableServiceLinks: false
      serviceAccountName: default
      automountServiceAccountToken: true
      hostIPC: false
      hostNetwork: false
      hostPID: false
      dnsPolicy: ClusterFirst
      imagePullSecrets: 
        - name: cr-spydersoft-github
      containers: 
        - env:
          - name: POSTGRES_PORT
            value: "5432"
          - name: POSTGRES_HOST
            value: postgres-service
          - name: POSTGRES_DB
            value: identity
          envFrom:
          - secretRef:
              name: postgres-secrets
          image: postgres:17
          imagePullPolicy: IfNotPresent
          name: main
          resources:
            requests:
              cpu: 5m
          volumeMounts:
          - mountPath: /var/lib/postgresql/data
            name: postgres-data
          - mountPath: /docker-entrypoint-initdb.d/init-identity-user.sh
            name: postgres-init-data
            subPath: init-data.sh
      volumes: 
        - name: postgres-data
          persistentVolumeClaim:
            claimName: identity-data
        - configMap:
            name: identity-data
          name: postgres-init-data
