---
# Source: ha-isy-wrapper/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "ha-isy-wrapper-test-connection"
  labels:
    helm.sh/chart: ha-isy-wrapper-0.1.0
    app.kubernetes.io/name: ha-isy-wrapper
    app.kubernetes.io/instance: ha-isy-wrapper
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['ha-isy-wrapper:']
  restartPolicy: Never
