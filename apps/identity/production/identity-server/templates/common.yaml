---
# Source: identity-server/templates/common.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-server-identity
  labels:
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
data:
  AllowedOrigins__0: https://*.mattgerega.com
  Identity__AuthorityUrl: https://auth.mattgerega.com
  Serilog__MinimumLevel__Default: Warning
  Telemetry__Log__Otlp__Endpoint: http://production-grafana-alloy.grafana:4317
  Telemetry__Metrics__Otlp__Endpoint: http://production-grafana-alloy.grafana:4317
  Telemetry__Trace__Otlp__Endpoint: http://production-grafana-alloy.grafana:4317
---
# Source: identity-server/templates/common.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-server-postgres-init-data
  labels:
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
data:
  init-data.sh: |
    #!/bin/bash
    set -e;
    if [ -n "${POSTGRES_NON_ROOT_USER:-}" ] && [ -n "${POSTGRES_NON_ROOT_PASSWORD:-}" ]; then
      psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
        CREATE USER "${POSTGRES_NON_ROOT_USER}" WITH PASSWORD '${POSTGRES_NON_ROOT_PASSWORD}';
        GRANT ALL PRIVILEGES ON DATABASE ${POSTGRES_DB} TO "${POSTGRES_NON_ROOT_USER}";
        ALTER DATABASE ${POSTGRES_DB} OWNER TO "${POSTGRES_NON_ROOT_USER}";
    EOSQL
    else
      echo "SETUP INFO: No Environment variables given!"
    fi
---
# Source: identity-server/templates/common.yaml
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: identity-server
  labels:
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "300Gi"
  storageClassName: "nfs-client"
---
# Source: identity-server/templates/common.yaml
apiVersion: v1
kind: Service
metadata:
  name: identity-server-main
  labels:
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/service: identity-server-main
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/name: identity-server
---
# Source: identity-server/templates/common.yaml
apiVersion: v1
kind: Service
metadata:
  name: identity-server-postgres
  labels:
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/service: identity-server-postgres
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/controller: postgres
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/name: identity-server
---
# Source: identity-server/templates/common.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-server-main
  labels:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/controller: main
      app.kubernetes.io/name: identity-server
      app.kubernetes.io/instance: identity-server
  template:
    metadata:
      annotations: 
        checksum/configMaps: 437248e113ff8eabcc72d3a5a182af10baef76c85abf4e4a1b1c5135b48342ff
      labels: 
        app.kubernetes.io/controller: main
        app.kubernetes.io/instance: identity-server
        app.kubernetes.io/name: identity-server
    spec: 
      enableServiceLinks: false
      serviceAccountName: default
      automountServiceAccountToken: true
      hostIPC: false
      hostNetwork: false
      hostPID: false
      dnsPolicy: ClusterFirst
      imagePullSecrets: 
        - name: cr-spydersoft-github
      containers: 
        - envFrom:
          - configMapRef:
              name: identity-server-identity
          - secretRef:
              name: identity-server-secrets
          image: ghcr.io/spyder007/identity_server:3.0.1
          imagePullPolicy: IfNotPresent
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /livez
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 1
          name: main
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 1
          volumeMounts:
          - mountPath: /postgres-secrets
            name: postgres-secrets
      volumes: 
        - name: postgres-secrets
          secret:
            secretName: postgres-secrets
---
# Source: identity-server/templates/common.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-server-postgres
  labels:
    app.kubernetes.io/controller: postgres
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/controller: postgres
      app.kubernetes.io/name: identity-server
      app.kubernetes.io/instance: identity-server
  template:
    metadata:
      annotations: 
        checksum/configMaps: 437248e113ff8eabcc72d3a5a182af10baef76c85abf4e4a1b1c5135b48342ff
      labels: 
        app.kubernetes.io/controller: postgres
        app.kubernetes.io/instance: identity-server
        app.kubernetes.io/name: identity-server
    spec: 
      enableServiceLinks: false
      serviceAccountName: default
      automountServiceAccountToken: true
      hostIPC: false
      hostNetwork: false
      hostPID: false
      dnsPolicy: ClusterFirst
      imagePullSecrets: 
        - name: cr-spydersoft-github
      containers: 
        - env:
          - name: POSTGRES_PORT
            value: "5432"
          - name: POSTGRES_HOST
            value: postgres-service
          - name: POSTGRES_DB
            value: identity
          envFrom:
          - secretRef:
              name: postgres-secrets
          image: postgres:17
          imagePullPolicy: IfNotPresent
          name: main
          volumeMounts:
          - mountPath: /var/lib/postgresql/data
            name: postgres-data
          - mountPath: /docker-entrypoint-initdb.d/init-identity-user.sh
            name: postgres-init-data
            subPath: init-data.sh
          - mountPath: /postgres-secrets
            name: postgres-secrets
      volumes: 
        - name: postgres-data
          persistentVolumeClaim:
            claimName: identity-server
        - configMap:
            name: identity-server-postgres-init-data
          name: postgres-init-data
        - name: postgres-secrets
          secret:
            secretName: postgres-secrets
---
# Source: identity-server/templates/common.yaml
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: identity-server
  labels:
    app.kubernetes.io/instance: identity-server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: identity-server
    app.kubernetes.io/version: 1.0.9
    helm.sh/chart: identity-server-0.1.0
  namespace: production
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main
      namespace: envoy-gateway-system
  hostnames:
    - "auth.mattgerega.com"
  rules:
    - backendRefs:
      - group: ""
        kind: Service
        name: identity-server-main
        namespace: production
        port: 80
        weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
